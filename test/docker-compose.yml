version: '3.8'

services: 
    test:
        image: "knoriy/test"
        build: "./"
        container_name: test
        # volumes:
        # - /home/$USER:/home/
        # restart: always
        # ports:
        # - "5000:5000"
        # environment:
        # - type=lab
        